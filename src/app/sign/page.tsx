import {LoginForm} from "@/app/sign/log-in-form";

export default function SignPage() {
    return <div
        className="container relative h-[800px] flex-col items-center justify-center grid lg:max-w-none lg:px-0">
        <LoginForm/>
    </div>
}